# OCR Project Requirements
# Updated for unified OCR router and cloud OCR support

# PyTorch with CUDA support for GPU acceleration (TrOCR)
# Install PyTorch first: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
--find-links https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# Transformers (for TrOCR handwriting OCR)
transformers>=4.44.0

# OCR Libraries
# PyTesseract (requires Tesseract binary installation separately)
pytesseract>=0.3.8

# Image processing
opencv-contrib-python>=4.10.0.84
numpy>=2.0.0
pillow>=11.0.0

# PDF processing (optional - for PDF support in ocr_router.py and ollama_cloud_ocr.py)
pdf2image>=1.16.3

# Ollama client (for LLM-based OCR)
ollama>=0.1.7

# Additional utilities
requests>=2.31.0
urllib3>=2.0.0

# Development and testing (optional)
# pytest>=7.4.0
# pytest-cov>=4.1.0

# Windows-specific dependencies (optional)
# comtypes==1.4.9
# pywin32==306

# Installation Notes:
# 1. Create virtual environment: python -m venv ollama-ocr_env
# 2. Activate: .\ollama-ocr_env\Scripts\Activate.ps1
# 3. Install PyTorch first: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# 4. Install Tesseract binary separately (not via pip) - download from: https://github.com/UB-Mannheim/tesseract/wiki
# 5. Install remaining requirements: pip install -r requirements.txt
#
# Note: PyTorch dependencies (filelock, fsspec, Jinja2, etc.) will be installed automatically
# Note: pdf2image requires poppler - install separately on Windows